name: 2048 Build
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: build project
        run: |
          cd 2048
          echo $pwd
          npm install
          npm build
